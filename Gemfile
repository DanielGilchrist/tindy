source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'httparty'
gem 'retries'

group :test do
  gem 'rake'
  gem 'turn'
  gem 'vcr'
  gem 'webmock'
end
